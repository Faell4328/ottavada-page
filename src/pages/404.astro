---
import NotFound from "~/components/NotFound.astro";
import { getPathLocale, DEFAULT_LOCALE } from "~/i18n";
const locale = getPathLocale(Astro.url.pathname) ?? DEFAULT_LOCALE;
---
<NotFound locale={locale} />
